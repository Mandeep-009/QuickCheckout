name: Example Workflow

on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Testing Secret
        env:
          MY_SECRET: ${{ secrets.FIREBASE_CONFIG }}
        run: |
          echo $MY_SECRET > ../../secret.json
